name: Hello Workflow
on:
  workflow_dispatch:
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from Github Actions!"
      - name: Print current directory
        run: pwd 
      - name: list files
        run: ls -la