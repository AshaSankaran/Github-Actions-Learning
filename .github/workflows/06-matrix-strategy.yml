name: Matrix Strategy
on:
  workflow_dispatch:
jobs:
  matrix-job:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Print Operating System
        run: echo "Running on ${{ matrix.os }}"